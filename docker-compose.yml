# Compose for Arcanada Assistant (dev + prod).
# All bindings are loopback-only by design (SEC-0006). Production reaches
# Postgres/Redis through the docker network only — never via the host's
# public interface. Secrets MUST be Vault-injected via .env on deploy.

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: assistant_app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required (Vault-injected)}
      POSTGRES_DB: assistant
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # ARCA-0039: strips SUPERUSER/CREATEROLE/CREATEDB from assistant_app.
      # Only executes on first boot against an empty volume — see
      # ops/postgres-init/01-restrict-role.sql header + README § Security.
      - ./ops/postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U assistant_app -d assistant']
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      [
        'redis-server',
        '--requirepass',
        '${REDIS_PASSWORD:?REDIS_PASSWORD required (Vault-injected)}',
      ]
    ports:
      - '127.0.0.1:6379:6379'
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli -a "$$REDIS_PASSWORD" --no-auth-warning ping']
      interval: 5s
      timeout: 3s
      retries: 10
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD required (Vault-injected)}

  assistant:
    build:
      context: .
      dockerfile: apps/assistant/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: development
      PORT: 3800
      LOG_LEVEL: info
      DATABASE_URL: postgresql://assistant_app:${POSTGRES_PASSWORD}@postgres:5432/assistant?schema=public&sslmode=disable
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/3
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-changeme}
      TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET:-changemechangemechangemechangeme}
      TELEGRAM_WEBHOOK_PATH: /webhook/telegram
      AUTH_ARCANA_BASE_URL: https://auth.arcanada.one
      AUTH_ARCANA_JWKS_URL: https://auth.arcanada.one/.well-known/jwks.json
      AUTH_ARCANA_JWT_ISSUER: https://auth.arcanada.one
      AUTH_ARCANA_JWT_AUDIENCE: arcanada-assistant
      MODEL_CONNECTOR_BASE_URL: http://connector.arcanada.one:3900
      # /health probe target — the mesh :3900 port is unreachable from this
      # container's network, so probe the public nginx surface (no auth).
      MODEL_CONNECTOR_HEALTH_URL: ${MODEL_CONNECTOR_HEALTH_URL:-https://connector.arcanada.one}
      MODEL_CONNECTOR_DEFAULT_MODEL: anthropic/claude-haiku-4-5
      MODEL_CONNECTOR_API_KEY: ${MODEL_CONNECTOR_API_KEY:-changeme}
      # INFRA-0240: hostname `arcana-db` not resolvable inside docker (no MagicDNS); fall back to Tailscale IP for arcana-db. Override via .env on hosts с разной топологией.
      SCRUTATOR_BASE_URL: ${SCRUTATOR_BASE_URL:-http://100.70.137.104:8310}
      SCRUTATOR_LTM_NAMESPACE: assistant-ltm-pavel
      # AGENT-0210: Ops Bot moved off this host to arcana-agents on 2026-08-16,
      # which killed the previous default (the opsbot_default docker network and
      # its container-name URL) — a service name only resolves while both
      # containers share a host. The public host cannot substitute BY DESIGN:
      # ops.arcanada.ai resolves to Cloudflare, so a request arrives from a CF
      # address and nginx rejects /metrics via its mesh allowlist, which is what
      # keeps the ecosystem snapshot off the public internet. The default is
      # therefore the mesh address; not a secret, so it lives here in plain
      # sight. http:// is accepted for mesh hosts by url-schemas.ts.
      OPSBOT_BASE_URL: ${OPSBOT_BASE_URL:-http://100.90.7.20:3600}
      OPSBOT_API_KEY: ${OPSBOT_API_KEY:-changeme}
      # A2-281: the canonical Muneral address (AGENTS.md § Operating rules).
      # `https://muneral.com` was the SITE, not the API — every call under it
      # 404s, and the token beside it was the literal string `changeme`.
      MUNERA_BASE_URL: ${MUNERA_BASE_URL:-https://api.muneral.com/api/v1}
      # A2-281: the agent key is read from a FILE, never from the environment.
      # The path below is the read-only mount added at the bottom of this
      # service; a missing file degrades every Muneral read to a marker naming
      # the cause, it does not fake an empty board.
      MUNERAL_AGENT_KEY_FILE: ${MUNERAL_AGENT_KEY_FILE:-/run/secrets/muneral-agent-key}
      # Optional: narrows the proactive reads to one Muneral project.
      MUNERAL_PROJECT_ID: ${MUNERAL_PROJECT_ID:-}
      # INFRA-0240: mesh-auth API key (Zod .optional(); placeholder satisfies min(1) Zod check для dev/test).
      MESH_VAULT_API_KEY: ${MESH_VAULT_API_KEY:-arc_api_placeholder_dev_not_set_value_padding_padding}
      BRIEFING_TIMEZONE: Europe/Istanbul
      BRIEFING_MORNING_TIME: '08:00'
      BRIEFING_EVENING_TIME: '21:00'
      # ARCA-0010 proactive config — file-backed (Vault SDK deferred per task-description § Implementation Notes #3).
      # Module loads YAML at boot + 5-min poll; missing path ⇒ module idle.
      PROACTIVE_CONFIG_PATH: /etc/arcanada/proactive-config.yaml
    ports:
      - '127.0.0.1:3800:3800'
    networks:
      - default
    volumes:
      # Read-only bind for the proactive-config directory. Host dir deployed by
      # operator (see datarim/tasks/ARCA-0010-task-description.md § Operator
      # Handoff). Dir-bind (not file-bind) is intentional: prevents Docker from
      # silently creating an empty directory at the file path on dev hosts
      # where /etc/arcanada/proactive-config.yaml is absent — ProactiveModule
      # then catches ENOENT and stays idle (graceful degradation).
      - /etc/arcanada:/etc/arcanada:ro
      # A2-281: the KB bind `/data/arcanada-kb:ro` was REMOVED with the reader
      # that used it. The rsync that filled it came from arcana-agents, which
      # INFRA-0417 decommissioned on 2026-09-17; the host directory has been
      # empty since, so the briefing and the digest rendered «источник
      # недоступен» in every section, every day. Work items live in Muneral.
      #
      # Read-only bind for the Muneral agent key. Dir-bind, same rationale as
      # /etc/arcanada above: a missing host directory must not be silently
      # created as an empty one at the FILE path, which would turn "no key" into
      # "unreadable key" and cost a deploy to diagnose. The host file is
      # /etc/arcanada/secrets/muneral-agent-key, mode 0400, owned by the
      # container user; it is never in git and never in the environment.
      - ${MUNERAL_SECRETS_DIR:-/etc/arcanada/secrets}:/run/secrets:ro

networks:
  default: {}
  # AGENT-0210: `opsbot_default` (external: true) was removed here. It was
  # created by the Ops Bot compose project on this host; Ops Bot moved to
  # arcana-agents on 2026-08-16, so nothing creates it any more. An `external`
  # network is never created by compose — it is only looked up — so the entry
  # had become a latent hard failure: the network survived solely because the
  # assistant container was still attached to it, and the first `compose up`
  # after a full stop would have died with "network opsbot_default declared as
  # external, but could not be found". Ops Bot is reached over the mesh address
  # in OPSBOT_BASE_URL above, so the network is no longer needed for anything.

volumes:
  postgres-data:
